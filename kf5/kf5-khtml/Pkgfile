# Description : Web rendering engine, based on the KParts technology and using KJS for JavaScript support
# URL: https://www.kde.org/
# Packager: pierre at nutyx dot org, fanch at nutyx dot org, tnut at nutyx dot org
# Depends on: extra-cmake-modules qt5 giflib kf5-kjs kf5-kparts libjpeg-turbo libpng  
name=kf5-khtml
release=1

_name=khtml
source=(http://download.kde.org/stable/frameworks/${version%.*}/portingAids/${_name}-$version.tar.xz)

build() {
cd ${_name}-$version
mkdir build
cd    build

cmake -DCMAKE_INSTALL_PREFIX=/usr    \
      -DCMAKE_BUILD_TYPE=Release            \
      -DSYSCONF_INSTALL_DIR=/etc/kf5 \
      -DLIB_INSTALL_DIR=lib                 \
      -DBUILD_TESTING=OFF                   \
      -DQT_PLUGIN_INSTALL_DIR=lib/qt5/plugins \
      -DECM_MKSPECS_INSTALL_DIR=/usr/share/qt5/mkspecs/modules \
      -Wno-dev .. 

make
make DESTDIR=$PKG install
}
