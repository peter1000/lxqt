# Description : The Kate package contains an advanced text editor for the KDE Plasma Desktop
# URL: https://www.kde.org/
# Packager: pierre at nutyx dot org, fanch at nutyx dot org, tnut at nutyx dot org
# Depends on: kf5-kinit kf5-knewstuff kf5-ktexteditor kf5-kitemmodels kf5-plasma-framework 
name=kde5-kate
release=1
version=15.04.3

source=(http://download.kde.org/stable/applications/$version/src/kate-$version.tar.xz)

build() {
cd kate-$version
mkdir build
cd build
cmake -DCMAKE_INSTALL_PREFIX=/usr  \
      -DCMAKE_BUILD_TYPE=Release          \
      -DLIB_INSTALL_DIR=lib               \
      -DBUILD_TESTING=OFF                 \
      -DQT_PLUGIN_INSTALL_DIR=lib/qt5/plugins \
      -Wno-dev ..
make
make DESTDIR=$PKG install
}
