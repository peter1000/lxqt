# Description : Library that allows you to have global accelerators that are independent of the focused window
# URL: https://www.kde.org/
# Packager: pierre at nutyx dot org, fanch at nutyx dot org, tnut at nutyx dot org
# Depends on: kf5-kconfig kf5-kcrash kf5-kdbusaddons kf5-ki18n extra-cmake-modules qt5  
name=kf5-kglobalaccel
release=1

_name=kglobalaccel
source=(http://download.kde.org/stable/frameworks/${version%.*}/${_name}-$version.tar.xz)

build() {
cd ${_name}-$version
mkdir build
cd    build
cmake -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DLIB_INSTALL_DIR=lib \
    -DKDE_INSTALL_USE_QT_SYS_PATHS=ON \
    -DBUILD_TESTING=OFF                \
    -DECM_MKSPECS_INSTALL_DIR=/usr/share/qt5/mkspecs/modules \
      ..
make
make DESTDIR=$PKG install
}
