# Description : The KPeople package contains a library that provides unified access to user's contacts from different sources, grouping them by person while still exposing all the data.
# URL: https://www.kde.org/
# Packager: pierre at nutyx dot org, fanch at nutyx dot org, tnut at nutyx dot org
# Depends on: kf5-kitemviews kf5-kservice kf5-kwidgetsaddons 
name=kf5-kpeople
release=1
_name=kpeople
source=(http://download.kde.org/stable/frameworks/${version%.*}/kpeople-$version.tar.xz)

build() {
cd kpeople-$version
mkdir build
cd    build

cmake -DCMAKE_INSTALL_PREFIX=/usr 		\
      -DCMAKE_BUILD_TYPE=Release         \
      -DLIB_INSTALL_DIR=lib              \
      -DBUILD_TESTING=OFF                \
      -DQML_INSTALL_DIR=lib/qt5/qml      \
      -DECM_MKSPECS_INSTALL_DIR=/usr/share/qt5/mkspecs/modules \
      ..
make
make DESTDIR=$PKG install
}
